export const NavBar = (props) => {
  return (
    <nav className=" bg-dark flex items-center justify-between bg-accent">
      <div className="logo text-primary">Expense Tracker</div>
      <div className="avatar bg-accent">BK</div>
    </nav>
  );
};
